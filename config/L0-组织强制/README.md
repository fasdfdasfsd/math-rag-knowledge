# L0 — 组织强制层

> 状态：个人项目不使用

## 说明

L0 层由企业 IT 统一管控，位于系统目录（如 `/etc/claude/` 或组策略路径）。
个人项目和开源项目**不需要**此层。

## 何时使用

- 企业统一要求代码审计日志
- 组织级别的 API 端点强制（如必须走企业网关）
- 合规红线（如禁止访问外部模型、数据不出境）

## 个人项目

跳过 L0，从 L1（用户全局）开始配置。
